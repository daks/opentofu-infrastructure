# Terraform / Opentofu infrastructure

Basic code used for basic needs mostly.
Or to re-learn and try things with Opentofu.

This code is probably of no use except for me, or for examples.

## Usage

start with `tofu init` to install plugins.
